# Airflow UID for file permissions (Linux/Mac)
# On Windows, you can leave this as is
AIRFLOW_UID=50000

# Airflow admin credentials
_AIRFLOW_WWW_USER_USERNAME=admin
_AIRFLOW_WWW_USER_PASSWORD=admin

# Project directory
AIRFLOW_PROJ_DIR=.
